import { FC } from "react";

interface NextQuizMessageProps {
  query: string;
}

export const NextQuizMessage: FC<NextQuizMessageProps> = ({ query }) => {
  return (
    <div>
      <div></div>
    </div>
  );
};
